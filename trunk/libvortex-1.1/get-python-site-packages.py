#!/usr/bin/python

import sys

basedir = sys.path[1]
# print "Base directory: %s" % basedir

# check if basedir
site_packages = "%s/%s" % (basedir, "sites-packages")
for path in sys.path:
    if site_packages == path:
        print "%s" % path
        sys.exit (0)

site_packages = "%s/%s" % (basedir, "dist-packages")
for path in sys.path:
    if site_packages == path:
        print "%s" % path
        sys.exit (0)



